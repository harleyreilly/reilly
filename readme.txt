git config --global user.name "reilly"
git config --global user.email "reillycena@gmail.com"


mkdir hello-world
cd hello-world
git init
(go to the folder, create a file on text editor(readme.txt----add text))

git add readme.txt
git commit -m "Comment"

(add another txt and save on text editor to see the difference...then..)

git diff

(add and commit it again)

(open your github account)

git config --global user.username "harley reilly"

(create a repo on your account)

git remote add origin (url)/ get set-url (url)


git push origin master



sasasa






